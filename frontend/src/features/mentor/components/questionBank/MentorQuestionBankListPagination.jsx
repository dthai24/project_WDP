import AppPagination from '@/shared/ui/AppPagination';

export const QB_LIST_PAGE_SIZE = 8;

export default function MentorQuestionBankListPagination(props) {
  return <AppPagination {...props} />;
}
